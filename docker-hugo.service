[Unit]
Description=Redis Container
Requires=docker.service
After=docker.service

[Service] 
Restart=always
ExecStart=/usr/bin/docker run -it -d --restart unless-stopped -v /var/www/html/hugopages:/src -p 1313:1313     klakegg/hugo:0.101.0    server -D  --bind 192.168.1.4

[Install]
WantedBy=default.target